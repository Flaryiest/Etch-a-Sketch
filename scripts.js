const box = document.querySelector("div");
const newGrid = document.querySelector(".newGrid");
newGrid.addEventListener("click", function()  {createGrid()})

function createGrid() {
    let squares = prompt("Enter # of squares per row (Limit 100)")
}

